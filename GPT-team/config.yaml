# ============================================================
# 自动回调脚本 配置文件
# ============================================================

# 注册账号总数
total_accounts: 1
proxy:                          # 代理（可选）
  http: "127.0.0.1:7890"
# ------------------------------------------------------------
# 临时邮箱 API 配置
# ------------------------------------------------------------
temp_mail:
  worker_domain: "mail.joini.cloud"
  email_domains:
    - "joini.cloud"
  admin_password: "jwtsdawfauhwfiolahifwhiaj2026"

# ------------------------------------------------------------
# Sub2Api 配置
# ------------------------------------------------------------
sub2api:
  base_url: "https://code.joini.cloud"
  bearer: ""
  email: "admin@sub2api.local"
  password: "d7281f144d22954ba7e1bda91c2b8bdc"
  auto_upload_sub2api: true
  group_ids: [2]

  
# ------------------------------------------------------------
# CLI Proxy API (CPA) 配置（旧版兼容，gpt-team-new.py 当前不再使用）
# ------------------------------------------------------------
cli_proxy:
  management_url: "http://xxx.comn:8317/management.html#/oauth"
  password: "xxxxxx"
  api_base: "http://xxx.comn:8317"
  upload_enabled: true    # 旧版开关，当前 gpt-team-new.py 以上传到 sub2api 为准


# ------------------------------------------------------------
# 输出文件
# ------------------------------------------------------------
output:
  accounts_file: "accounts.txt"        # 格式：email|password|时间（一行一个）
  invite_tracker_file: "invite_tracker.json"
  results_file: "results.txt"           # get_tokens.py 输出：email|email_jwt|access_token


# ------------------------------------------------------------
# 车头（Team）配置
# 只需填 email 和 max_invites，account_id/auth_token 自动获取
# password 可不填 → 自动使用邮箱验证码登录
# 当前版本默认使用 temp_mail.admin_password 按邮箱地址接收母号 OTP
# jwt 字段已非必填，保留仅为兼容旧配置
# ------------------------------------------------------------
teams:
  - name: "tp1"
    # email: "58m2174s@joini.cloud"
    email: "toayxianshen155@outlook.com"
    password: ""
    jwt: ""   # 可留空；当前默认使用 temp_mail 全局配置收 OTP
    max_invites: 4


